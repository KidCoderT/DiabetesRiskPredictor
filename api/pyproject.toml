[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "joblib",
    "lightgbm",
    "scikit-learn==1.1.3",
    "numpy<2",
]

[dependency-groups]
dev = [
    "pytest",
    "httpx",
]
